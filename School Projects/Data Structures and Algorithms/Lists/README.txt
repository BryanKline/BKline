
Lists

Brief:

Folders contain two different types of linked list implementations, 
array based and node based.

Detailed:

The folders contain two different types of linked list implementations, 
array based and node based.  Both implementations support inserting into 
and removing from the list, navigating through the list with a cursor, 
as well as overloaded operators for assignment and display.
